# Training API (`deconvolve.training`)

The `deconvolve.training` package contains network architectures, the JAX training engine, and the validation MMD computation.

---

## Models (`deconvolve.training.models`)

::: deconvolve.training.models
    options:
      show_root_heading: true
      show_source: true

---

## MMD (`deconvolve.training.mmd`)

::: deconvolve.training.mmd
    options:
      show_root_heading: true
      show_source: true

---

## Training Engine (`deconvolve.training.engine`)

::: deconvolve.training.engine
    options:
      show_root_heading: true
      show_source: true
      members:
        - train
        - TrainResult
        - TrainState
